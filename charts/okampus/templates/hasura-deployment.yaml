apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hasura.name" . }}
  labels:
    {{- include "hasura.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels: {{- include "hasura.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      creationTimestamp: null
      labels: {{- include "hasura.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - image: hasura/graphql-engine:v2.22.1
          imagePullPolicy: IfNotPresent
          name: {{ .Values.hasura.service.portName }}
          env:
            - name: HASURA_GRAPHQL_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "okampus.postgresSecret" . }}
                  key: PSQL_CONNECTION_STRING
            - name: HASURA_GRAPHQL_METADATA_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "hasura.postgresSecret" . }}
                  key: HASURA_METADATA_CONNECTION_STRING
            - name: HASURA_GRAPHQL_ENABLE_CONSOLE
              value: "true"
          ports:
            - name: {{ .Values.hasura.service.portName }}
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            failureThreshold: 10
            periodSeconds: 10
            timeoutSeconds: 2
            httpGet:
              path: /healthz
              port: {{ .Values.hasura.service.portName }}
          startupProbe:
            failureThreshold: 15
            periodSeconds: 5
            timeoutSeconds: 2
            httpGet:
              path: /healthz
              port: {{ .Values.hasura.service.portName }}
          resources: {{- toYaml .Values.hasura.resources | nindent 12 -}}

